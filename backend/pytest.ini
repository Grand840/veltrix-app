[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

markers =
    integration: Tests d integration (necessitent Docker en ligne)
    unit: Tests unitaires (SQLite en memoire)
